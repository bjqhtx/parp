#内置函数
http://edu.51cto.com/lesson/id-92357.html

数值处理   
	rand()  返回0,1之间的随机数

字符串处理
	length([s]):返回指定字符串的长度
	sub(r,s,[t]):以r表示的模式来查找t所表示的字符串中的匹配内容，并将其第一次出现替换为s所表示的内容
	gsub(r,s,[t]):替换所有
	split(s,a[,r]):以r为分隔符，切割字符串s，结果保存在a中
  